ALTER TABLE hospitals ADD COLUMN IF NOT EXISTS featured BOOLEAN NOT NULL DEFAULT FALSE;
ALTER TABLE hospitals ADD COLUMN IF NOT EXISTS featured_until TIMESTAMP(6) WITH TIME ZONE;
ALTER TABLE hospitals ADD COLUMN IF NOT EXISTS image_data TEXT;
ALTER TABLE blood_banks ADD COLUMN IF NOT EXISTS featured BOOLEAN NOT NULL DEFAULT FALSE;
ALTER TABLE blood_banks ADD COLUMN IF NOT EXISTS featured_until TIMESTAMP(6) WITH TIME ZONE;
ALTER TABLE blood_banks ADD COLUMN IF NOT EXISTS image_data TEXT;
ALTER TABLE donation_events ADD COLUMN IF NOT EXISTS featured BOOLEAN NOT NULL DEFAULT FALSE;
ALTER TABLE donation_events ADD COLUMN IF NOT EXISTS featured_until TIMESTAMP(6) WITH TIME ZONE;
ALTER TABLE donation_events ADD COLUMN IF NOT EXISTS event_time TIME;
ALTER TABLE donation_events ADD COLUMN IF NOT EXISTS sponsor_name VARCHAR(255);
ALTER TABLE donation_events ADD COLUMN IF NOT EXISTS sponsor_logo_url VARCHAR(1000);

CREATE TABLE IF NOT EXISTS payments (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id BIGINT NOT NULL,
    purpose VARCHAR(40) NOT NULL,
    target_id BIGINT,
    amount INTEGER NOT NULL,
    transaction_uuid VARCHAR(255) NOT NULL UNIQUE,
    product_code VARCHAR(100) NOT NULL,
    status VARCHAR(20) NOT NULL,
    esewa_ref VARCHAR(255),
    created_at TIMESTAMP(6) WITH TIME ZONE NOT NULL,
    completed_at TIMESTAMP(6) WITH TIME ZONE,
    CONSTRAINT fk_payment_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    CONSTRAINT ck_payment_amount CHECK (amount > 0)
);

CREATE INDEX IF NOT EXISTS idx_payment_user ON payments(user_id, created_at);
CREATE INDEX IF NOT EXISTS idx_payment_status ON payments(status);
